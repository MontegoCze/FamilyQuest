SECRET_KEY=change-this-secret-key-in-production
ALGORITHM=HS256
ACCESS_TOKEN_EXPIRE_MINUTES=60
DATABASE_URL=sqlite:///./familyquest.db
BACKEND_CORS_ORIGINS=["http://localhost:5173","http://localhost:3000"]
# Production: replace the local origins with the deployed frontend URL.
FRONTEND_URL=http://localhost:5173
# Required for sending family invitations.
SMTP_HOST=
SMTP_PORT=587
SMTP_USERNAME=
SMTP_PASSWORD=
SMTP_FROM_EMAIL=
SMTP_USE_TLS=true
